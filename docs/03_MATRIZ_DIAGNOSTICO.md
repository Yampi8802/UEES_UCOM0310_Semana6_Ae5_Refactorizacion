# Fase I | Matriz de diagnóstico obligatoria

| # | Ubicación | Smell / problema | Categoría | Impacto | Candidato | Prueba necesaria |
|---:|---|---|---|---|---|---|
| 1 |ServicioReservas.procesar() |La clase concentra validaciones, cálculo de tarifa, persistencia simulada, notificación y confirmación de la reserva. |Responsabilidades excesivas / Long Class conceptual |Un cambio en cualquiera de estas responsabilidades puede obligar a modificar la misma clase. |Separar responsabilidades|Pruebas de reserva válida, inválida, cálculo de tarifa y confirmación. |
|  2 | `ServicioReservas.procesar()` en `r.getCorreo()`, `r.getInicio()`, `r.getFin()` y `r.getTipo()` | `ServicioReservas` obtiene varios datos internos de `Reserva` para tomar decisiones y realizar cálculos.              | Feature Envy                                        | El servicio depende de varios detalles de `Reserva`, aumentando el acoplamiento entre las clases.              | Evaluar Move Method                     | Pruebas que comprueben que las validaciones y el cálculo mantienen exactamente el comportamiento actual. |
|  3 | `Reserva.tipo` y condición `"VIP".equals(r.getTipo())`                                          | El tipo de reserva se representa mediante un `String` y el servicio compara directamente el texto `"VIP"`.            | Primitive Obsession                                 | Un valor textual representa una decisión del dominio y puede permitir valores no controlados.                  | Crear Value Object o tipo de dominio    | Pruebas para NORMAL, VIP y un tipo diferente, verificando que los totales no cambien.                    |
|  4 | Bloque de validaciones al inicio de `ServicioReservas.procesar()`                               | Varias condiciones independientes controlan si el proceso continúa o retorna `0`.                                     | Condicionales / lógica de validación concentrada    | La lógica de validación está mezclada con el cálculo, persistencia simulada y notificación.                    | Extraer responsabilidades de validación | Pruebas para correo inválido, período inválido y anticipación insuficiente.                              |
|  5 | `return 0` en las cuatro validaciones de `ServicioReservas.procesar()`                          | Diferentes errores de entrada producen el mismo retorno `0`, sin distinguir la causa.                                 | Resultado ambiguo / manejo de errores               | El consumidor del método no puede diferenciar por el retorno si falló el correo, el período o la anticipación. | Mejorar representación del resultado    | Pruebas individuales para cada entrada inválida y verificación del resultado observable.                 |
|  6 | `40` y `0.85` dentro de `ServicioReservas.procesar()`                                           | Los valores de tarifa base y descuento VIP están escritos directamente dentro del método.                             | Magic Numbers                                       | Un cambio en una tarifa o descuento requiere modificar código y puede dificultar su mantenimiento.             | Extraer constantes                      | Pruebas de NORMAL y VIP para comprobar que los totales continúan siendo 40.0 y 34.0.                     |

## Observación

No se identifican como problemas confirmados `Long Parameter List` ni `Data Clumps`, porque el método `procesar()` solamente recibe dos parámetros y el código mostrado no presenta grupos repetidos de datos que justifiquen esos diagnósticos.
